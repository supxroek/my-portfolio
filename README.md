
  # Portfolio redesign request

  This is a code bundle for Portfolio redesign request. The original project is available at https://www.figma.com/design/Xae4XcxzEvnbuvdI6vM6NS/Portfolio-redesign-request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  